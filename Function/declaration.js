/*
    Topics:
        A. Normal function declaration (add, sub)

*/
// --------------------------------------------------------------------------->>

// Add numbers
function add(a, b) {
  const result = a + b;
  //   console.log(result);
}
add(5, 9); // 14
add(12, 20); // 32

//
// Sub number:
function sub(a, b) {
  const result = a - b;
  //   console.log(result);
}
sub(12, 8); // 4
sub(58, 8); // 50

// --------------------------------------------------------------------------->>
