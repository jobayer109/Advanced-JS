import Child from "./Child";

const result = new Child("Green", 5, 10);
console.log(result);

// result.calculate(); // 50
// result.height; // 5

// result.color; // Green

// result.draw(); // Drawing.

// console.log(result); //Child {color: 'Green', height: 5, width: 10}
