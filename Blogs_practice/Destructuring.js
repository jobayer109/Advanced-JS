// Array destructure
const myArray = [1, 2, 3];
const [first, second, jobayer] = myArray;
first; // 1
second; // 2
jobayer; // 3

//
// Object destructure
const myObject = { x: 1, y: 2, z: 3 };
const { x, y } = myObject;
x; // 1
y; // 2
